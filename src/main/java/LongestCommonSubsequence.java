import java.util.List;

public class LongestCommonSubsequence {

    // The list of Strings, all length k each containing a permutation of the first k letters of the alphabet
    private final List<String> strings;

    // The number of characters in each string in strings
    private final int k;

    public LongestCommonSubsequence( List<String> strings, int k ) {
        this.strings = strings;
        this.k = k;
    }

    public int solve( ) {

        // TODO: Implement your solution here.

        return 0;
    }
}
